{
  "name": "buzz-import-roster",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Imports the Swing Buzz participant roster from Google Sheets into Firestore. Roster fields only — never touches balances or check-in state.",
  "scripts": {
    "headers": "node index.mjs headers",
    "import": "node index.mjs import",
    "set-role": "node index.mjs set-role",
    "seed-drinks": "node index.mjs seed-drinks",
    "test": "node --test"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "googleapis": "^144.0.0"
  }
}

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Per-user Xcode state. The shared scheme in xcshareddata IS committed, so
# `xcodebuild -scheme BuzzTerminal` works on a fresh clone.
xcuserdata/

# Swift Package Manager
.build/
.swiftpm/
# Package.resolved IS committed. That convention ("ignore it") is for libraries,
# whose consumers resolve their own versions. This is an application: pinning the
# exact Firebase graph means a fresh clone, and a future CI, build the same bytes.

# Node (the roster importer)
node_modules/
npm-debug.log*

# Firebase. Never commit these: the plist identifies the project, and a
# service-account key is full admin access to it.
GoogleService-Info.plist
serviceAccountKey.json
*-serviceAccount*.json
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# macOS
.DS_Store

# Local scratch
screenshots/
